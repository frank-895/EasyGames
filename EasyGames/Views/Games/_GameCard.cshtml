<!-- Partial generated by ChatGPT -->
@model EasyGames.Models.Game

<div class="product-card">
    @if (User.IsInRole("Admin"))
    {
        <form asp-controller="Games" asp-action="Delete" asp-route-id="@Model.Id" method="post" class="product-card__delete" onsubmit="return confirm('Are you sure you want to delete this item?');">
            <button type="submit" class="product-card__delete-btn" aria-label="Delete">&times;</button>
        </form>
    }
    <div class="product-card__media">
        <img src="@(string.IsNullOrWhiteSpace(Model.ImageUrl) ? Url.Content("~/images/placeholder.svg") : Model.ImageUrl)"
             alt="@Model.Title"
             class="product-card__img" />
    </div>

    <div class="product-card__body">
        <h3 class="product-card__title">@Model.Title</h3>

        @if (!string.IsNullOrEmpty(Model.Platform))
        {
            <div class="product-card__meta">@Model.Platform</div>
        }

        @if (!string.IsNullOrEmpty(Model.AgeRating))
        {
            <div class="product-card__meta product-card__meta--muted">@Model.AgeRating</div>
        }

        <div class="product-card__footer">
            <div class="product-card__price">
                @Model.Price.ToString("C")
            </div>
            <div class="product-card__stock">
                <span class="badge bg-secondary">Stock: @Model.StockQuantity</span>
            </div>
        </div>

        <div class="product-card__actions">
            <a asp-controller="Games" asp-action="Details" asp-route-id="@Model.Id" class="btn btn-sm btn-outline-primary">Details</a>
            @if (User.IsInRole("Admin"))
            {
                <a asp-controller="Games" asp-action="Edit" asp-route-id="@Model.Id" class="btn btn-sm btn-outline-secondary">Edit</a>
            }
        </div>
    </div>
</div>
